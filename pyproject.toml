[project]
name = "bookshelf"
version = "0.1.0"
description = "Self-hosted ebook library — local test tooling for Calibre-Web Automated"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fpdf2>=2.8.7",
    "playwright>=1.60.0",
    "python-dotenv>=1.2.2",
]
